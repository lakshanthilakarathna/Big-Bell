## VPS helpers

These helpers are optional; they’re meant to make an Ubuntu VPS deploy less error-prone.

- `nginx-ip-only.conf`: Nginx server block template for serving the site by IP.
- `setup_ubuntu.sh`: prints the commands to install system packages and prepare `/var/www/firbrigs`.

